# install 會覆寫為 64 字元十六進位亂數。自訂值至少 16 字元，可使用英數及 . _ ~ ! @ % + , : / -。
POSTGRES_PASSWORD=replace-with-openssl-rand-hex-32
# NEXDROP_CURSOR_SECRET 由 install 產生並在 update 時保留；直接使用 Compose 時須自行設定至少 32 字元穩定亂數。
NEXDROP_CURSOR_SECRET=replace-with-openssl-rand-hex-32
NEXDROP_IMAGE=ghcr.io/honguan/nexdrop:2.0.1
NEXDROP_DOMAIN=localhost
NEXDROP_BOOTSTRAP_ADMIN_USERNAME=admin
NEXDROP_BOOTSTRAP_ADMIN_EMAIL=admin@example.com
# 至少 12 字元；支援英數及 . _ ~ ! @ % + , : / -。install 會覆寫為隨機值。
NEXDROP_BOOTSTRAP_ADMIN_PASSWORD=replace-with-a-random-admin-password
NEXDROP_LOGIN_RATE_LIMIT_PER_MINUTE=10

# 設備加入節點時使用；install 會產生。舊版 .env 缺少此欄位時，update 會自動產生並永久保留。
NEXDROP_NODE_KEY=replace-with-random-node-key
# Web 管理員 OTP Base32 密鑰；install 會產生。舊版 .env 缺少此欄位時，update 會自動產生並套用至尚未啟用 OTP 的管理員。
NEXDROP_BOOTSTRAP_ADMIN_TOTP_SECRET=replace-with-base32-secret
# 可選，逗號分隔允許存取的來源 IP/CIDR。
NEXDROP_ALLOWED_IPS=
